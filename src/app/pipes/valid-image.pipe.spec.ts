import { ValidImagePipe } from './valid-image.pipe';

describe('ValidImagePipe', () => {
  it('create an instance', () => {
    const pipe = new ValidImagePipe();
    expect(pipe).toBeTruthy();
  });
});
